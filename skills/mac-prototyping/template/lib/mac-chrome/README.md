# lib/mac-chrome (placeholder)

populated at scaffold time from the toolkit's packages/mac-chrome

The files here are a minimal stub so the template typechecks, renders, and
tests standalone. When creating a prototype, copy the toolkit's
`packages/mac-chrome` over this directory (see the toolkit's
`references/workflows.md`); if the package is not available, the stub keeps
everything working. Keep imports pointed at `lib/mac-chrome/index.ts` — the
vendored package preserves that entry point.
