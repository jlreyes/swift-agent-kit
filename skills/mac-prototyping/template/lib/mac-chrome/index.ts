// PLACEHOLDER — populated at scaffold time from the toolkit's packages/mac-chrome
// (copy that package over this directory; see the toolkit's references/workflows.md).
// The stub implements exactly the surface the template pages use, with the real
// package's prop shapes, so vendoring is a drop-in replacement.
export {
  DesktopShell,
  MacDock,
  MacToolbar,
  SystemSymbol,
  TrafficLights,
  WindowChrome,
} from "./stub.tsx";
export type { DesktopShellProps, DockItem } from "./stub.tsx";
